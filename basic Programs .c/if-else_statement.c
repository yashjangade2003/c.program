#include<stdio.h>
#include<conio.h>

int main()
{
        int n;
        prientf("Enter any number");
        scanf("%d",&n);

        if(n>=0)
        {
            printf("positive number");
        }
        else
        {
            printf("Negative number");
        }
        getch();
        return 0;
}
