#include<stdio.h>
#include<conio.h>

int main ()
    {
       int a[5],i,x=0

       for(i=0,i<5,i++)
       {
           scanf("%d",&a[i]);
       }
       for(i=;i<5,i++)
       {
           if(x>a[i])
           {

           }
           else
           {
               (x=a[i]);
           }
       }
       printf("%d",x);
    }
    getch();
    return 0;
